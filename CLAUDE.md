# Cheaty Flashcards

See [README.md](./README.md) for the documentation index and [PLAN.md](./PLAN.md) for the redesign plan/phase history — both are kept brief on purpose; follow their links to `docs/*.md` for whatever the current task actually needs, rather than reading everything up front.

This project's toolchain is **Vite+** (`vp`), not plain `pnpm`/`npm` scripts directly — see [docs/toolchain.md](./docs/toolchain.md) before running install/check/test commands.
