# Open questions / revisit later

Genuinely open items only. Answered questions whose reasoning is recorded elsewhere (plan-notes, [design-decisions.md](./design-decisions.md), [game-vision.md](./game-vision.md)) get removed rather than accumulating here.

- Exact Leitner stage count and promotion/demotion thresholds — start simple (0–5), tune based on how it feels in play. Act 2 raises the stakes on these numbers: stage ≥3 is now also "card won" and stage 5 is "gold" (see [game-vision.md](./game-vision.md)), so retuning one retunes the other.
- Vite+ is beta and evolving fast — re-check `viteplus.dev/guide/migrate` and `viteplus.dev/guide/upgrade` before each new phase in case the CLI surface has changed.
- **Phase 8 — `FactFamilyShape` visual design**: shipped as a CSS `clip-path` triangle reusing `stageBgClass()`; add vs. multiply distinction is currently just the displayed numbers. Still open — and now Phase 14's problem, since the shape becomes a card face with won/gold/at-risk states.
- **Phase 8 — localStorage schema break**: dropping old progress data on upgrade is fine while the only player is family (no migration written). Precedent explicitly carried into Act 2 by [game-vision.md](./game-vision.md); Phase 13's wallet will be the next schema break.
- **Phase 9 — `/settings` route**: not yet — settings is still one boolean (sound). Revisit when a second setting shows up; Phase 17's "grown-ups corner" (focus-numbers input, export/import) is the likeliest trigger.
- **Phase 9 — sound asset license**: the eight mp3s are ElevenLabs free-tier (non-commercial). Fine for now — no ads/payments/data collection anywhere — and handled by disclosure on `/about`. If the project ever monetizes, the sounds are the thing to regenerate.
- **Phase 9 — Ziggy's typewriter reveal**: whether kids find the crawl charming or in the way of pressing Play. Capped at 1.8s, skipped under `prefers-reduced-motion`; cheapest fix is tap-to-finish before dropping the effect. Worth an explicit look during Phase 15 (more Ziggy lines mid-session makes this matter more).
- **Phase 12 — table size and the hard cap**: `TABLE_MAX_SEATS = 6` (12 questions at the floor) and `SESSION_MAX_QUESTIONS = 24` are tuned against a simulation, not against a kid — see the measured table in [plan-notes/phase-12.md](./plan-notes/phase-12.md). The open question is whether 12–18 questions actually feels like ~2 minutes in a real sitting, especially with 2.6s Ziggy reveals in the mix. Watch it during Phase 17's device pass.
- **Phase 12 — a deal deferred by leaving the page**: the unlock check is scoped to the table just played, which lives in `PlayPage` component state — so navigating away before pressing Advance defers the new card to the end of the next session rather than losing it. Persisting card movements (Phase 14) is the real fix; revisit there rather than patching it now.
